# GBIF 中国区域子集（辅助，非主分类）
#
# raw/  — 放置 GBIF SIMPLE_CSV（解压后的 csv/tsv/txt）
#
#   GBIF_USER=... GBIF_PASSWORD=... GBIF_EMAIL=... npm run enrich:gbif -- --request
#   npm run enrich:gbif
#   npm run enrich:gbif -- --name="Chrysolophus pictus"
#   npm run species:publish
